services:
  mysql:
    image: mysql:8.0
    container_name: rbsoftware-mysql
    restart: "no"
    environment:
      MYSQL_DATABASE: rbsoftware
      MYSQL_USER: rbsoftware
      MYSQL_PASSWORD: rbsoftware
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - mysql_data_prod:/var/lib/mysql
    command: --default-authentication-plugin=mysql_native_password
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-proot"]
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 30s

  minio:
    image: minio/minio:latest
    container_name: rbsoftware-minio
    restart: "no"
    command: server /data --console-address ":9001"
    env_file: infra/.env
    volumes:
      - minio_data_prod:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 60s
      timeout: 5s
      retries: 3
      start_period: 10s

  backend:
    build:
      context: ./backend
    container_name: rbsoftware-backend
    restart: unless-stopped
    mem_limit: 512m
    cpus: 1.0
    env_file:
      - ./backend/.env
    depends_on:
      - mysql
      - minio
    networks:
      - default
      - robotschool

  frontend:
    build:
      context: ./frontend
    container_name: rbsoftware-frontend
    restart: unless-stopped
    mem_limit: 256m
    cpus: 0.5
    depends_on:
      - backend
    networks:
      - default
      - robotschool

volumes:
  mysql_data_prod:
    external: true
    name: rbsoftware_rbsoftware_mysql_data
  minio_data_prod:
    external: true
    name: rbsoftware_rbsoftware_minio_data

networks:
  default:
  robotschool:
    external: true
    name: robotschool-inventory_robotschool
